<UserControl x:Class="SaaMedW.View.EditVisitView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:local="clr-namespace:SaaMedW.View"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Border Padding="10">
            <WrapPanel Grid.Row="0">
                <Label>Услуги</Label>
                <ComboBox MinWidth="300" ItemsSource="{Binding BenefitsList}" 
                          DisplayMemberPath="Name" 
                          SelectedValue="{Binding /}" SelectedItem="{Binding BenefitCurrent}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <i:InvokeCommandAction Command="{Binding RefreshGrid}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ComboBox>
            </WrapPanel>
        </Border>
        <Grid x:Name="slotGrid" Margin="5,10,5,10" MinHeight="400" Grid.Row="1">
        </Grid>
    </Grid>
</UserControl>
