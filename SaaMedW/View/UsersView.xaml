    <UserControl x:Class="SaaMedW.View.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SaaMedW.View"
             xmlns:local0="clr-namespace:SaaMedW"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local0:RoleToStringConverter x:Key="myConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ToolBar Grid.Row="0">
            <Button Command="{Binding Add}">
                Добавить
            </Button>
            <Button Command="{Binding Edit}">
                Изменить
            </Button>
            <Button Command="{Binding Del}">
                Удалить
            </Button>
            <Button Command="{Binding ChangePassword}">
                Сменить пароль
            </Button>
        </ToolBar>
        <DataGrid Grid.Row="1" Margin="5" ItemsSource="{Binding UsersList}" 
                  SelectedItem="{Binding UsersSel}" IsSynchronizedWithCurrentItem="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="50"></DataGridTextColumn>
                <DataGridTextColumn Header="ФИО" Binding="{Binding Fio}" Width="150"></DataGridTextColumn>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="100"></DataGridTextColumn>
                <DataGridTextColumn Header="Роль" Binding="{Binding Path=Role,Converter={StaticResource myConverter}}" Width="*">
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
